---
name: skill-evaluation
description: "按失效层评估 agent skill：触发、写作、身份、口径、承诺、方法论、执行；深度实跑验证。"
---

# Agent Skill Quality Review

评价一个 skill = 检查它在哪些层会失效，每层绑定检测动作与证据。写作质量只是其中一层。

---

## 核心定位

**这个 review 是干什么的：**

当你或其他开发者编写了新的 agent skill（Claude、Qoder、LangChain、AutoGen 或其他 LLM 框架），想知道它会在哪个层面失效、哪里会坏、怎么修时，用这个技能做评估。

**核心信念：**

- 评价 skill 的资格来自"能把问题检查出来"，前提是**知道 skill 会在哪些层出问题**
- 每个失效层必须绑定一个检测动作——只命名关注点、不规定检查动作的不算检查过
- 评价的输出永远不是"没问题"，而是"检查过这些层、用的这些动作、结果如下"——宣称完备即不称职
- 失效清单是活的：每发生一次真实事故，回流成一条检查点（`references/failure-log.md`）

**评估范围：**

- ✅ **多 Agent 框架通用**：Claude Code Skills、Qoder Skills、LangChain Tools、AutoGen Agents、以及其他 LLM 应用
- ✅ **快速模式纯静态**：触发层、写作质量层、身份层、口径层、承诺层（存在性）
- ✅ **深度模式加实跑**：七层全检，含方法论层审计与执行层实跑

**不是什么：**

- ❌ 不是安全审计（那是 `skill-vetter` 的职责）
- ❌ 不是完整功能测试（深度模式只抽验 skill 自声明的命令，不做穷举测试）
- ❌ 不是代码质量评估（Python/JS 脚本写得好不好）
- ❌ 不承诺穷举所有失效——只承诺检查过的层如实报告

---

## 何时触发（使用条件）

当用户想评估一个 skill 写得好不好时，自动触发：

- "帮我评测一下这个 skill"
- "这个 skill 写得怎么样"
- "帮我看看 xx skill 写得好不好"
- "评估 skill <name>"
- "review skill"
- "skill review"
- 任何要求评价 skill 质量的中文/英文表达

---

## 失效分层模型

skill 的失效分七层。评价按层进行，模式分界 = 检测动作的成本：

| 层 | 失效形状 | 检测动作 | 成本 | 快速 | 深度 |
|----|---------|---------|------|------|------|
| **触发层** | 误触 / 漏触 | trigger_words 静态规则；`test_triggers.py` 实测 | 低/中 | 静态 | +实测 |
| **写作质量层** | 描述含糊、结构缺失、指令歧义 | 维度二/三/四规则（引擎可算） | 低 | ✓ | ✓ |
| **身份层** | 定位段自声明与正文矛盾 | 定位段 vs 工作流逐条对读 | 低 | ✓ | ✓ |
| **口径层** | 多文档/多处数字与说法不一 | 定向 grep + 数字复算 | 低 | ✓ | ✓ |
| **承诺层** | 引用的文件/命令不存在 | 文件存在性检查；`--help` 实跑 | 低/中 | 存在性 | +实跑 |
| **方法论层** | 工作流步骤无主 | 每步找主（命令/被委派文档/AI 自身） | 中 | ✗ | ✓ |
| **执行层** | 声明的命令跑不通、退出码与承诺不符 | 实跑 + 边界探针 | 高 | ✗ | ✓ |

**证据纪律（贯穿所有层）：**

- 每个检查点粘贴实际执行的命令 + 退出码 + 输出摘录；"能跑"、"graceful fail" 这类无输出话术不算证据
- 无法验证的项如实标 ⏸ 未验证并写明原因，不得给分
- 分数必须能追溯到规则条目——现场编的小数（如 4.7）按无源数字处理
- 指控某层失效前必须现造现跑探针——读代码不算，看输出才算

---

## 评估流程

### Step 0: 判断 skill 类型

先读 skill 的 SKILL.md，**判断它是什么类型的 skill**：

| 类型 | 特征 | 评估重点 |
|------|------|---------|
| **工具型** | 有明确的输入输出，执行动作 | 执行层 > 写作质量层 |
| **咨询型** | 提供建议、引导对话 | 方法论层 > 执行层 |
| **混合型** | 两者兼有 | 所有层都重要 |

### Step 1: 询问评估模式（强制执行）

❗ **必须执行的步骤**：在提供任何评估结论前，先问用户选择题：

```markdown
你想怎么评估？

A) **快速评估** - 静态五层检查（触发/写作/身份/口径/承诺）+ 静态四维分 + Top 3 问题  
B) **深度评估** - 快速评估 + 方法论层与执行层实跑验证（七层全检）；要实跑被测 skill 的命令，耗时可能较长

(选 A 或 B)
```

**规则：**
- 用户说"A"/"快速" → 快速评估
- 用户说"B"/"深度" → 深度评估
- 用户没回复/说随便 → 默认选 A
- 时间只做定性说明：快速评估纯静态；深度评估要实跑被测 skill 的命令，耗时可能较长——都不写具体分钟数
- **禁止跳过此步直接开始评估**

### Step 2: 确定 skill 路径

如果用户没有提供 skill 路径：
- 尝试从对话上下文推断（如用户提到某个 skill 名称）
- 如果不确定，问用户："你想评估哪个 skill？"

### Step 3: 逐层检查

快速模式先跑引擎，机械覆盖触发层静态、写作质量层、承诺层存在性、口径层机械项：

```bash
python3 scripts/evaluate_skill.py <skill-path> [--output json]
```

身份层对读、方法论层审计、执行层实跑是你的动作，引擎不替代。

#### 触发层（静态 = 原维度一）

**检查项：**

1. **是否存在：** `_meta.json` 是否有 `trigger_words` 字段

2. **数量评估：**
   - 1-10 个：合理
   - 11-15 个：偏多，建议精简
   - >15 个：过多，必须精简
   - 0 个：严重问题，没有触发机制

3. **质量评估：**
   检查以下问题词：
   ```
   过于通用 → 误触风险高
   "帮我" | "试试" | "test" | "help" | "make" | "do" | "请"
   长度 ≤ 2 → 精确度不足（除非是专业缩写如 AI/UI/API）
   ```

4. **打分：**
   | 情况 | 分数 |
   |------|------|
   | 1-10 个，无问题词 | 5 |
   | 1-10 个，有 1-2 个问题词 | 4 |
   | 11-15 个，无问题词 | 4 |
   | >15 个 | 3 |
   | 11-15 个，有问题词 | 3 |
   | 0 个 | 1 |
   | 有明显误触词 | 2 |

**深度模式追加（触发层实测）：**

运行 `scripts/test_triggers.py <skill-path>`（方法详见 `references/trigger-testing.md`），记录命中率 / 误触率 / 漏触率：

- 实测结果是本层的实证依据：静态分与实测冲突时以实测为准（有误触按锚点表降分；正例全命中且零误触可上调 0.5，上限 5）
- 报告中必须粘贴脚本输出表格，只写"触发正常"不算做过
- 已知局限：脚本内置正例只硬编码了 brainstorming / marketing / hallmark 三个 skill，评其他 skill 需 `--query-file` 自带正例，否则命中率 0/0 无意义

#### 写作质量层（原维度二/三/四）

**描述清晰度（维度二）：**

1. **字数统计：** description 的字符数（不含 frontmatter）

2. **内容质量：**
   - 是否一句话说明 skill 做什么
   - 是否包含"当用户...的时候"等冗余句式
   - 是否包含"等"字（暗示列举不全）
   - 是否有逗号连接的复合句（超过 3 个）

3. **打分：**
   | 字数 | 分数 |
   |------|------|
   | ≤50 字 | 5 |
   | 51-100 字 | 4 |
   | 101-200 字 | 3 |
   | >200 字 | 2 |
   | 空 | 0 |

   **扣分项：**
   - 包含"当用户...的时候"：-0.3
   - 包含"等"字：-0.5
   - 逗号连接复合句 >3 个：-0.5

**结构完整性（维度三）：**

| 检查项（每项 1 分） | 判断方法 |
|--------|---------|
| name 字段 | frontmatter 有 `name:` |
| description 字段 | frontmatter 有 `description:` |
| 前置条件 | 有"何时使用"/"When to Use"/"使用条件"章节 |
| 边界情况 | 有"边界"/"红线"/"不适用"/"Anti-Pattern"/"禁止"章节 |
| 示例 | 有"示例"/"example"章节或示例代码 |
| 相关 skill | 有"相关"/"Related"/"see also"章节 |

```
分数 = (符合项数 / 6) × 5，保留 1 位小数
```

**指令可操作性（维度四）：**

1. **模糊词汇检测：**
   ```
   高风险模糊词（每出现一次 -0.5）：
   "适当" | "适时" | "必要时" | "酌情" | "视情况"

   低风险模糊词（每出现一次 -0.3）：
   "考虑" | "建议" | "可以" | "或许"
   ```

2. **清晰词汇检测（加分项）：**
   ```
   每出现一次 +0.2：
   "必须" | "执行" | "运行" | "调用"
   每出现一次 +0.3：
   "禁止" | "不得" | "绝不"
   ```

3. **矛盾检测：**
   - 搜索"必须"和"可以"在同一段落出现
   - 搜索方向相反的指令（如"先 A 后 B"和"先 B 后 A"）

4. **打分：**
   ```
   基础分 = 3.0
   加上清晰词汇得分
   减去模糊词汇扣分
   如有矛盾，直接 ≤2
   最终分数在 0-5 之间
   ```

#### 身份层

对读"核心定位 / 不是什么"与正文工作流，判定只有三种：**一致 / 存疑 / 矛盾**（矛盾时引用两处原文）：

- 定位段声明"不做 X"、正文却要求或承诺 X → 矛盾，高优先级
- 定位段声明"只做静态"、正文要求实跑 → 矛盾，除非定位段明确按模式分拆（"快速静态 / 深度实跑"是合法写法）
- 教训指针：v3.0 曾在定位写"纯文档层面/不是功能测试"、正文维度五却要求"实际跑 skill"——执行者按定位段执行，实跑被静默跳过（failure-log #2）

#### 口径层

数字与说法的唯一真源检查，逐项复算：

- **权重复算：** 各处权重表加总 = 1；快速版与深度版公式逐项一致；同一权重多处出现时数字相同
- **徽章复算：** README 徽章 / benchmark 数字 = 引擎实测输出，不是四舍五入往上凑
- **元数据同步：** `_meta.json` 与 frontmatter 的 description / name 一致；版本号出现在 CHANGELOG
- **多文档对读：** 中英（或多份）文档定位一致
- 教训指针：17.5% 权重与 0.2/0.2/0.2/0.25/0.15 打架、4.8 徽章 vs 自评 2.5（failure-log #2、#4）

#### 承诺层

- 提取 SKILL.md 与 README 引用的 `scripts/`、`references/` 文件，逐一存在性检查（引擎自动做；外部绝对路径不算本库承诺）
- 深度模式：核心命令 `--help` 实跑确认接口存在
- 教训指针：目录树里的 meta_template.md（实际是 meta-template.md）、CONTRIBUTING 引用的 requirements.txt 与 tests/ 均不存在（failure-log #3）

#### 方法论层（仅深度）

拆出 skill 承诺的工作流，**逐步找主**。主有三种：**命令**（有实跑证据）、**被委派文档**（skill 正文明裁给某文档/skill）、**AI 自身**（按某方法论文档执行）。

- **委派有效：** 仲裁 / 边界声明裁过的步骤不算断链，但要求就地或就近章节可发现
- **判"无主/断链"前必须读完全文的仲裁、委派、边界章节**——否则是假阳性（failure-log #6：把仲裁表裁给 finesse-ui/impeccable 的"生成"步骤误判断链）
- 声明"不做"的边界 ≠ 断链
- **分档：**
  | 情况 | 分数 |
  |------|------|
  | 每步有主且就地可发现 | 5 |
  | 每步有主，个别委派需翻全文才能发现 | 4 |
  | 步骤有主但主不存在（指向的文档/命令缺失） | 3（承诺层同时扣） |
  | 存在无主步骤（真断链） | 2 |
  | 未做方法论审计 | 不得给分，标注"深度评估未完成" |

#### 执行层（仅深度）

实跑 skill 自声明的每条命令（工具型），或拿一个真实案例按 SKILL.md 指令走一遍、对照文档承诺的产物形状（咨询型）：

- 正常路径 + 至少一个边界探针（非法参数 / 文件不存在）
- 退出码与文档承诺的契约逐项核对——注意**同名不同义**：0/1/2 在不同脚本含义可能不同，按各自契约判
- 旧维度五三个检查项在本层的落点：概念完整性 = 正常路径能否实现；边界处理 = 边界探针行为；错误恢复 = 错误路径实测
- **分档：**
  | 情况 | 分数 |
  |------|------|
  | 全部命令实跑通过、退出码契约吻合 | 5 |
  | 命令能跑但有边界行为与文档不符 | 4 |
  | 有命令跑不起来 | 2 |
  | 未实跑 | 不得给分，标注"深度评估未完成" |

---

### Step 4: 评分

**静态四维分（快速模式，引擎输出）：**

```
静态四维分 = 触发 × 0.25 + 描述 × 0.25 + 结构 × 0.25 + 可操作性 × 0.25
```

只覆盖触发层（静态）与写作质量层。身份 / 口径 / 承诺以层清检表呈现（通过 / 存疑 / 失效），不进数字。

**深度综合分（七层加权）：**

| 层 | 权重 | 分数来源 |
|----|------|---------|
| 触发层 | 0.15 | 静态分 + 实测修正 |
| 写作质量层 | 0.15 | (描述 + 结构 + 可操作性) / 3 |
| 身份层 | 0.10 | 一致 = 5；存疑 = 3；矛盾 = 1 |
| 口径层 | 0.10 | 通过 = 5；存疑 = 3；失效 = 1 |
| 承诺层 | 0.10 | 通过 = 5；部分缺失 = 3；幽灵文件 = 1 |
| 方法论层 | 0.20 | Step 3 分档 |
| 执行层 | 0.20 | Step 3 分档 |

方法论与执行权重最高：评价一个 skill 的核心，是它承诺的东西真的能发生。

---

### Step 5: 问题优先级

把发现的问题按严重程度分类：

| 级别 | 判断标准 | 处理方式 |
|------|---------|---------|
| **高** | 阻断性问题，功能无法正常使用 | 必须修复才能发布 |
| **中** | 影响使用体验 | 建议修复 |
| **低** | 优化建议 | 可以之后再说 |

**高优先级问题特征：**
- 没有 trigger_words
- description 为空
- 指令自相矛盾（身份层矛盾）
- 缺少前置条件导致无法使用
- 引用的文件/命令不存在（承诺层失效）
- 工作流存在无主步骤（方法论层断链）

---

### Step 6: 生成报告

按以下格式输出报告（层清检表是主体，不是分数）：

```markdown
# Skill 质量评估报告：<skill-name> @ <date>

## 基本信息
- **类型：** 工具型 / 咨询型 / 混合型
- **路径：** ~/.agents/skills/<name>/
- **评估模式：** 快速 / 深度

## 层清检表

| 层 | 检测动作 | 结果 | 证据 |
|----|---------|------|------|
| 触发层 | 静态规则 / test_triggers.py | ✓ / ⚠ / ✗ / ⏸ | <输出摘录> |
| 写作质量层 | 引擎四维 | ✓ / ⚠ | <分数> |
| 身份层 | 定位段对读 | 一致 / 存疑 / 矛盾 | <两处原文引用> |
| 口径层 | grep + 复算 | ✓ / ⚠ / ✗ | <复算过程> |
| 承诺层 | 存在性 / --help | ✓ / ⚠ / ✗ | <缺失清单> |
| 方法论层 | 工作流找主 | ✓ / ✗ / ⏸ | <步骤-主对照表> |
| 执行层 | 实跑 + 探针 | ✓ / ⚠ / ✗ / ⏸ | <命令+退出码> |

（快速模式：方法论 / 执行为 ⏸ 未检；触发层只有静态部分）

## 静态四维分：X/5

| 维度 | 分数 | 状态 |
|------|------|------|
| 触发准确性 | X | ✓ 良好 / ⚠ 需改进 |
| 描述清晰度 | X | ✓ 良好 / ⚠ 需改进 |
| 结构完整性 | X | ✓ 良好 / ⚠ 需改进 |
| 指令可操作性 | X | ✓ 良好 / ⚠ 需改进 |

## 深度综合分：X/5（仅深度模式）

<七层加权明细表>

## 问题列表

### 高优先级（必须修复）
1. **[高·<层名>]** <问题描述>
   - 现状：<具体表现>
   - 建议：<修复方法>

### 中优先级（建议修复） / 低优先级（可选优化）
...

## 证据附录（深度模式必附）

<逐检查点：实际命令 + 退出码 + 输出摘录>
```

---

## 示例对话

### 示例 1：快速评估

**用户：** 帮我评测 brainstorming skill

**我：** （询问评估模式）

> 你想怎么评估？
> - **快速评估**：静态五层检查 + 静态四维分 + Top 3 问题
> - **深度评估**：七层全检（要实跑命令，耗时可能较长）

**用户：** 快速就行

**我：** （跑引擎 + 身份层对读，按层清检表输出）

---

### 示例 2：跨框架评估

**用户：** 看看我的 LangChain 工具写得好不好

**我：** （确认是 Agent Tool/Tool 类型，用通用标准逐层检查）

---

### 示例 3：用户直接说 skill 名

**用户：** 评测 marketing-ideas

**我：** （直接用快速模式评估，因为用户没有明确要求深度）

---

### 示例 4：评估过程中发现问题

**我：** 评估发现 brainstorming 没有 `_meta.json`，无法配置触发词。这个优先级很高，需要先补充吗？

**用户：** 对，先把这个问题修好

**我：** （提供 _meta.json 模板）

---

## 相关文件

- `references/failure-log.md` — **事故回流入口**：每条真实事故固化成一条检查点，新失效先记这里再进正文
- `references/skill-rubric.md` — 评分锚点
- `references/trigger-testing.md` — 触发词测试方法
- `scripts/evaluate_skill.py` — 引擎（静态部分：触发/写作/承诺存在性/口径机械项）
- `scripts/test_triggers.py` — 触发层实测
