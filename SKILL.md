---
name: skill-evaluation
description: "评估其他 skill 的写作质量：触发词、描述、结构、指令可操作性。"
---

# Skill Evaluation

评估其他 skill 的写作质量，给出可操作的改进建议。

---

## 核心定位

**这个 skill 是干什么的：**

当你或团队写了一个新 skill，想知道它写得好不好、能不能用、有什么问题需要修复时，用这个 skill 做评估。

**不是什么：**

- 不是安全审计（那是 `skill-vetter` 的职责）
- 不是功能测试（skill 实际跑起来的效果）
- 不是代码质量评估（脚本写得好不好）

**评估的是：写作质量**——能不能让人看懂、触发是否准确、指令是否可执行。

---

## 何时触发

当用户想评估一个 skill 写得好不好时，自动触发：

- "帮我评测一下这个 skill"
- "这个 skill 写得怎么样"
- "帮我看看 xx skill 写得好不好"
- "评估 skill <name>"
- "review skill"
- "skill review"
- 任何要求评价 skill 质量的中文/英文表达

---

## 评估流程

### Step 0: 判断 skill 类型

先读 skill 的 SKILL.md，**判断它是什么类型的 skill**：

| 类型 | 特征 | 评估重点 |
|------|------|---------|
| **工具型** | 有明确的输入输出，执行动作 | 指令可操作性 > 触发准确性 |
| **咨询型** | 提供建议、引导对话 | 结构完整性 > 指令可操作性 |
| **混合型** | 两者兼有 | 所有维度都重要 |

### Step 1: 询问评估模式

> 你想怎么评估？
> - **快速评估**：5 分钟，静态分析 + 四维度打分 + Top 3 问题
> - **深度评估**：30 分钟，额外包含触发词测试 + 真实效果验证

用户没说默认选**快速评估**。

### Step 2: 确定 skill 路径

如果用户没有提供 skill 路径：
- 尝试从对话上下文推断（如用户提到某个 skill 名称）
- 如果不确定，问用户："你想评估哪个 skill？"

### Step 3: 快速评估（手工执行）

读取 skill 的 `SKILL.md` 和 `_meta.json`，逐维度分析。

#### 维度一：触发准确性

**检查项：**

1. **是否存在：** `_meta.json` 是否有 `trigger_words` 字段

2. **数量评估：**
   - 1-10 个：合理
   - 11-15 个：偏多，建议精简
   - >15 个：过多，必须精简
   - 0 个：严重问题，没有触发机制

3. **质量评估：**
   检查以下问题词：
   ```
   过于通用 → 误触风险高
   "帮我" | "试试" | "test" | "help" | "make" | "do" | "请"
   长度 ≤ 2 → 精确度不足（除非是专业缩写如 AI/UI/API）
   ```

4. **打分：**
   | 情况 | 分数 |
   |------|------|
   | 1-10 个，无问题词 | 5 |
   | 1-10 个，有 1-2 个问题词 | 4 |
   | 11-15 个，无问题词 | 4 |
   | >15 个 | 3 |
   | 11-15 个，有问题词 | 3 |
   | 0 个 | 1 |
   | 有明显误触词 | 2 |

#### 维度二：描述清晰度

**检查项：**

1. **字数统计：** description 的字符数（不含 frontmatter）

2. **内容质量：**
   - 是否一句话说明 skill 做什么
   - 是否包含"当用户...的时候"等冗余句式
   - 是否包含"等"字（暗示列举不全）
   - 是否有逗号连接的复合句（超过 3 个）

3. **打分：**
   | 字数 | 分数 |
   |------|------|
   | ≤50 字 | 5 |
   | 51-100 字 | 4 |
   | 101-200 字 | 3 |
   | >200 字 | 2 |
   | 空 | 0 |

   **扣分项：**
   - 包含"当用户...的时候"：-0.3
   - 包含"等"字：-0.5
   - 逗号连接复合句 >3 个：-0.5

#### 维度三：结构完整性

**检查项（每项 1 分）：**

| 检查项 | 判断方法 |
|--------|---------|
| name 字段 | frontmatter 有 `name:` |
| description 字段 | frontmatter 有 `description:` |
| 前置条件 | 有"何时使用"/"When to Use"/"使用条件"章节 |
| 边界情况 | 有"边界"/"红线"/"不适用"/"Anti-Pattern"/"禁止"章节 |
| 示例 | 有"示例"/"example"/"Example"章节或示例代码 |
| 相关 skill | 有"相关"/"Related"/"see also"/"Related Skills"章节 |

**打分：**

```
分数 = (符合项数 / 6) × 5，保留 1 位小数
```

#### 维度四：指令可操作性

**检查项：**

1. **模糊词汇检测：**
   ```
   高风险模糊词（每出现一次 -0.5）：
   "适当" | "适时" | "必要时" | "酌情" | "视情况"

   低风险模糊词（每出现一次 -0.3）：
   "考虑" | "建议" | "可以" | "或许"
   ```

2. **清晰词汇检测（加分项）：**
   ```
   每出现一次 +0.2：
   "必须" | "执行" | "运行" | "调用"
   每出现一次 +0.3：
   "禁止" | "不得" | "绝不"
   ```

3. **矛盾检测：**
   - 搜索"必须"和"可以"在同一段落出现
   - 搜索方向相反的指令（如"先 A 后 B"和"先 B 后 A"）

4. **打分：**
   ```
   基础分 = 3.0
   加上清晰词汇得分
   减去模糊词汇扣分
   如有矛盾，直接 ≤2
   最终分数在 0-5 之间
   ```

#### 维度五：真实效果（深度评估时）

**检查项：**

1. **概念完整性：** skill 描述的功能，实际能否实现
2. **边界处理：** 边界情况是否有处理
3. **错误恢复：** 出错时是否有指导

这是最难评估的维度，需要实际跑 skill 来验证。快速评估时跳过。

---

### Step 4: 综合评分

```
综合分 = 触发准确性 × 0.2 + 描述清晰度 × 0.2 + 结构完整性 × 0.2 + 指令可操作性 × 0.25 + 真实效果 × 0.15
```

**简化版（快速评估，不含真实效果）：**

```
综合分 = 触发准确性 × 0.25 + 描述清晰度 × 0.25 + 结构完整性 × 0.25 + 指令可操作性 × 0.25
```

**权重说明：**

| 维度 | 权重 | 原因 |
|------|------|------|
| 触发准确性 | 0.25 | 触发不对，一切都白搭 |
| 描述清晰度 | 0.25 | 第一印象，决定用户是否继续读 |
| 结构完整性 | 0.25 | 结构清晰才能用好 |
| 指令可操作性 | 0.25 | skill 的核心价值 |

---

### Step 5: 问题优先级

把发现的问题按严重程度分类：

| 级别 | 判断标准 | 处理方式 |
|------|---------|---------|
| **高** | 阻断性问题，功能无法正常使用 | 必须修复才能发布 |
| **中** | 影响使用体验 | 建议修复 |
| **低** | 优化建议 | 可以之后再说 |

**高优先级问题特征：**
- 没有 trigger_words
- description 为空
- 指令自相矛盾
- 缺少前置条件导致无法使用

---

### Step 6: 生成报告

按以下格式输出报告：

```markdown
# Skill 质量评估报告：<skill-name> @ <date>

## 基本信息
- **类型：** 工具型 / 咨询型 / 混合型
- **路径：** ~/.agents/skills/<name>/
- **评估模式：** 快速 / 深度

## 综合评分：X/5

| 维度 | 分数 | 状态 |
|------|------|------|
| 触发准确性 | X | ✓ 良好 / ⚠ 需改进 |
| 描述清晰度 | X | ✓ 良好 / ⚠ 需改进 |
| 结构完整性 | X | ✓ 良好 / ⚠ 需改进 |
| 指令可操作性 | X | ✓ 良好 / ⚠ 需改进 |
| 真实效果 | — | ⏸ 未评估（快速模式） |

## 结构扫描
- [✓/✗] name 字段
- [✓/✗] description 字段
- [✓/✗] 前置条件说明
- [✓/✗] 边界情况处理
- [✓/✗] 示例
- [✓/✗] 相关 skill 引用

## 问题列表

### 高优先级（必须修复）

1. **[高]** <问题描述>
   - 现状：<具体表现>
   - 建议：<修复方法>

### 中优先级（建议修复）

2. **[中]** <问题描述>
   - 现状：<具体表现>
   - 建议：<修复方法>

### 低优先级（可选优化）

3. **[低]** <问题描述>
   - 现状：<具体表现>
   - 建议：<修复方法>

## 改进清单

### 必须修复
- [ ] <任务 1>
- [ ] <任务 2>

### 建议优化
- [ ] <任务 3>
- [ ] <任务 4>
```

---

## 示例对话

### 示例 1：快速评估

**用户：** 帮我评测 brainstorming skill

**我：** （询问评估模式）

> 你想怎么评估？
> - **快速评估**：5 分钟，静态分析 + 四维度打分
> - **深度评估**：30 分钟，额外包含触发词测试 + 真实效果验证

**用户：** 快速就行

**我：** （读取 brainstorming，执行评估）

---

### 示例 2：用户直接说 skill 名

**用户：** 评测 marketing-ideas

**我：** （直接用快速模式评估，因为用户没有明确要求深度）

---

### 示例 3：评估过程中发现问题

**我：** 评估发现 brainstorming 没有 `_meta.json`，无法配置触发词。这个优先级很高，需要先补充吗？

**用户：** 对，先把这个问题修好

**我：** （提供 _meta.json 模板）
